{
  "name": "docsage",
  "version": "1.0.0",
  "description": "End-to-end AWS solution for uploading, storing, and analyzing documents with AI-powered insights",
  "main": "index.js",
  "scripts": {
    "build": "npm run build:backend && npm run build:frontend",
    "build:backend": "cd backend && npm install && npm run build",
    "build:frontend": "cd frontend && npm install && npm run build",
    "deploy": "cd infrastructure && cdk deploy",
    "destroy": "cd infrastructure && cdk destroy",
    "test": "npm run test:backend",
    "test:backend": "cd backend && npm test",
    "lint": "eslint . --ext .js,.ts",
    "start:dev": "cd frontend && npm start"
  },
  "keywords": [
    "aws",
    "serverless",
    "document-analysis",
    "textract",
    "comprehend",
    "s3",
    "lambda"
  ],
  "author": "DocSage Team",
  "license": "MIT",
  "devDependencies": {
    "eslint": "^8.0.0",
    "@typescript-eslint/eslint-plugin": "^5.0.0",
    "@typescript-eslint/parser": "^5.0.0"
  }
}